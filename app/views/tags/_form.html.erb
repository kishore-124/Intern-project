<%= bootstrap_form_with(model: tag, local: true) do |form| %>
  <% if tag.errors.any? %>
    <div id="alert alert-danger error_explanation"  class="alert alert-danger" role="alert">
      <h2><%= pluralize(tag.errors.count, "error") %> prohibited this tag from being saved:</h2>
      <ul>
        <% tag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group ">
  <div class="field">
    <%= form.text_field :name, class: "form-control", placeholder:"Please enter a valid Tag name" %>
  </div>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
